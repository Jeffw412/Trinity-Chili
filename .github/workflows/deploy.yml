# GitHub Pages deployment disabled - using Vercel instead
# This workflow has been disabled in favor of Vercel deployment
#
# Original workflow was for GitHub Pages static deployment
# Now using Vercel for server-side rendering deployment

name: Disabled - GitHub Pages Deploy

# Workflow disabled - no triggers
on: workflow_dispatch

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
    - name: Workflow Disabled
      run: echo "This workflow is disabled. Using Vercel for deployment instead."
