rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEVELOPMENT RULES - Very permissive for initial setup
    // Replace with production rules after setup is complete

    // Allow all operations on all collections during development
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
